<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Shadow Layer -->
    <!-- Main Card Background Layer -->
    <item >
        <shape android:shape="rectangle">
            <solid android:color="#D2EDEDED" /> <!-- White background color -->
            <corners android:radius="5dp" />
        </shape>
    </item>
    <item android:left="2dp" android:right="2dp" android:top="2dp" android:bottom="2dp">
        <shape android:shape="rectangle">
            <solid android:color="@color/white" /> <!-- Semi-transparent black for shadow -->
            <corners android:radius="5dp" /> <!-- Radius to match the main background -->
        </shape>
    </item>
</layer-list>
